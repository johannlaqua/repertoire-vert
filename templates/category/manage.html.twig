{% extends 'base_homepage.html.twig' %}



{% block body %}
    <div class="green-banner">
        <h3>Gestion des Catégories</h3>
    </div>
    <div class="row">
        <a class="btn" href="{{ url('categoryAdd') }}">Ajouter d'une categorie</a>
    </div>
    <table>
        <thead>
        <tr>
            <th>Nom</th>
            <th>Description</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.name }}</td>
                <td>{{ category.description }}</td>
                <td><a href="{{ url('categoryEdit',{'id':category.id}) }}">Edit</a>
                <a href="{{ url('categoryDelete',{id:category.id}) }}">Supprimer</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}