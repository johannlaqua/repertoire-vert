{% extends 'base_homepage.html.twig' %}

{% block title %}SucessStory{% endblock %}

{% block body %}
    <h1>SucessStory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sucess_story.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sucess_story.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sucess_story.description }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ sucess_story.lien }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ sucess_story.date ? sucess_story.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ sucess_story.photo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sucess_story_index') }}">back to list</a>

    <a href="{{ path('sucess_story_edit', {'id': sucess_story.id}) }}">edit</a>

    {{ include('sucess_story/_delete_form.html.twig') }}
{% endblock %}
